@model IEnumerable<UniversityManagementSystem.Models.Nationality>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NationalityName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NationalityName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NationalityId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.NationalityId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.NationalityId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
